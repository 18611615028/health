spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/health?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: root
server:
  port: 8888
mybatis:
  mapper-locations: classpath*:mapper/*.xml
logging:
  level:
    com.example.health.mapper: DEBUG
